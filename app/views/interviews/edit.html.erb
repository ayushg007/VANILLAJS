
<%= render 'form' %>
<div class="text-center edit-links">
  <%= link_to "Delete Interview", interview_path(@interview), method: :delete, data: { confirm: "Are you sure you want to delete this interview?" } %>
  |
  <%= link_to "Cancel", interviews_path %>
</div>